# Assets

`AppIcon.png`, `AppIcon.icns`, and the `AppIcon.iconset` files are generated
project assets for App Sweep. They are not derived from third-party product
logos or brand marks.

Unless replaced in a downstream fork, these assets are distributed under the
same MIT license as the source code.
